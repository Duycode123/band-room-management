// ====== BandRoom - Band Practice Room Booking System ======

Enum role {
  CUSTOMER
  STAFF
  ADMIN
}

Enum room_status {
  AVAILABLE
  IN_USE
  MAINTENANCE
}

Enum booking_status {
  PENDING_PAYMENT
  PAID
  CHECKED_IN
  COMPLETED
  CANCELLED
}

Enum payment_method {
  CASH
  ONLINE
}

Enum payment_provider {
  VNPAY
  COUNTER
}

Enum payment_transaction_status {
  INITIALIZED
  PENDING
  SUCCEEDED
  FAILED
  CANCELLED
}

Enum discount_type {
  PERCENTAGE
  FIXED_AMOUNT
}

Enum customer_issue_type {
  ROOM
  EQUIPMENT
  PAYMENT
  ACCOUNT
  OTHER
}

Enum customer_issue_report_status {
  OPEN
  IN_PROGRESS
  RESOLVED
  CLOSED
}

Enum shift_status {
  ASSIGNED
  IN_PROGRESS
  COMPLETED
}

Enum equipment_type {
  AMP
  MIXER
  MIC
  DRUM
  GUITAR
  KEYBOARD
  OTHER
}

Enum equipment_status {
  GOOD
  BROKEN
  MAINTENANCE
}

Table account {
  id              int       [pk, increment]
  email           varchar   [unique, not null]
  password_hash   varchar   [not null]
  role            role      [not null]
  created_at      timestamp [default: `now()`]
}

Table customer {
  id              int       [pk, increment]
  account_id      int       [unique, not null, ref: - account.id]
  full_name       varchar   [not null]
  phone_number    varchar
  email           varchar
  date_of_birth   date
}

Table staff {
  id              int       [pk, increment]
  account_id      int       [unique, not null, ref: - account.id]
  full_name       varchar   [not null]
  phone_number    varchar
  email           varchar
  date_of_birth   date
}

Table room_tier {
  id              int       [pk, increment]
  name            varchar   [not null]
  hourly_rate     decimal   [not null, note: 'Price per hour']
  description     text
}

Table room {
  id              int         [pk, increment]
  name            varchar     [not null]
  room_tier_id    int         [not null, ref: > room_tier.id]
  status          room_status [not null, default: 'AVAILABLE']
}

Table equipment {
  id              int              [pk, increment]
  room_id         int              [not null, ref: > room.id]
  type            equipment_type   [not null]
  name            varchar          [not null, note: 'Specific model name, e.g. Marshall MG30, Shure SM58']
  status          equipment_status [not null, default: 'GOOD']
  notes           text

  indexes {
    (room_id, type)
  }
}

Table discount_code {
  id                int           [pk, increment]
  code              varchar       [unique, not null]
  type              discount_type [not null]
  value             decimal       [not null]
  min_order_value   decimal       [note: 'Minimum order value to apply']
  expires_at        date
}

Table coupon_usage {
  id                bigint        [pk, increment]
  discount_code_id  int           [not null, ref: > discount_code.id]
  customer_id       int           [not null, ref: > customer.id]
  booking_id        int           [unique, not null, ref: - booking.id]
  discount_amount   decimal       [not null]
  used_at           timestamp     [not null, default: `now()`]

  indexes {
    (discount_code_id, used_at)
    (customer_id, used_at)
  }
}

Table booking {
  id                  int             [pk, increment]
  customer_id         int             [not null, ref: > customer.id]
  room_id             int             [not null, ref: > room.id]
  discount_code_id    int             [ref: > discount_code.id, note: 'Nullable']
  checkin_staff_id    int             [ref: > staff.id, note: 'Staff who handled check-in, nullable']

  start_time          timestamp       [not null]
  end_time            timestamp       [not null]
  checkin_time        timestamp
  checkout_time       timestamp

  payment_method      payment_method  [not null]
  applied_hourly_rate decimal         [not null, note: 'Hourly rate at the time of booking']
  total_price         decimal         [not null]
  status              booking_status  [not null, default: 'PENDING_PAYMENT']

  equipment_notes     varchar(500)    [note: 'Equipment/instruments the customer wants prepared']
  notes               varchar(500)    [note: 'Other notes from customer or staff']

  created_at          timestamp       [default: `now()`]

  indexes {
    (room_id, start_time, end_time) [note: 'Supports conflict checking']
  }
}

Table booking_status_history {
  id              int            [pk, increment]
  booking_id      int            [not null, ref: > booking.id]
  old_status      booking_status
  new_status      booking_status [not null]
  notes           varchar(500)
  changed_at      timestamp      [not null, default: `now()`]
  changed_by      int            [not null, ref: > account.id, note: 'account.id of the user who made the change']
}

Table payment_transaction {
  id                      int                        [pk, increment]
  booking_id              int                        [not null, ref: > booking.id]
  provider                payment_provider           [not null]
  transaction_reference   varchar(100)               [unique, not null]
  provider_transaction_id varchar(100)               [unique, note: 'Nullable provider-side transaction id']
  amount                  decimal                    [not null, note: 'Captured or expected payment amount']
  status                  payment_transaction_status [not null, default: 'INITIALIZED']
  response_code           varchar(20)
  paid_at                 timestamp
  created_at              timestamp                  [not null, default: `now()`]
  updated_at              timestamp                  [not null, default: `now()`]
}

Table review {
  id              int       [pk, increment]
  booking_id      int       [unique, not null, ref: - booking.id]
  rating          int       [not null, note: '1-5']
  content         text
  approved        boolean   [not null, default: false]
  created_at      timestamp [default: `now()`]
}

Table review_response {
  id              int       [pk, increment]
  review_id       int       [unique, not null, ref: - review.id, note: 'One admin response per review']
  responder_id    int       [not null, ref: > account.id]
  content         text      [not null]
  created_at      timestamp [default: `now()`]
  updated_at      timestamp [default: `now()`]
}

Table customer_issue_report {
  id              int                          [pk, increment]
  customer_id     int                          [not null, ref: > customer.id]
  booking_id      int                          [ref: > booking.id, note: 'Nullable when the issue is not tied to a specific booking']
  issue_type      customer_issue_type          [not null]
  description     varchar(1000)                [not null]
  status          customer_issue_report_status [not null, default: 'OPEN']
  created_at      timestamp                    [not null, default: `now()`]
  updated_at      timestamp                    [not null, default: `now()`]
}

Table shift {
  id              int          [pk, increment]
  staff_id        int          [not null, ref: > staff.id]
  date            date         [not null]
  start_time      time         [not null]
  end_time        time         [not null]
  status          shift_status [not null, default: 'ASSIGNED']
  quality_rating  int          [note: 'Admin rating for the shift, 1-5']
}
